# _archive

Stale or retired files go here. The audio site build never reads anything in a folder
whose name starts with "_" (this folder, articles/_template). To retire a whole
publication, move its article folder here, or set `status: archived` in its audio.yml.
